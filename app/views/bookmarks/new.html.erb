<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>

<p class="mt-2"><%= link_to "Back to list", @list, class: "btn btn-primary" %></p>
